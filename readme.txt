//This package provides statical calculations for binomial and normal distributed data sets
//The data sets must be in a .txt file with one entry per line.

//To install this package run the following commons inside of the folders directory

pip install .

//To test the package 

python test.py


